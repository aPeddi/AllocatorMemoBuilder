# ─────────────────────────────────────────────────────────────
# AllocatorMemoBuilder — dependencies.  Install with: ./amb setup
# (also installs backend/ as the editable amb_core package)
# Local-first: no web server (ADR-0009). The surface is the CLI + file exports.
# ─────────────────────────────────────────────────────────────

# Validation / config (structured outputs live here)
pydantic>=2
pydantic-settings
python-dotenv
pyyaml

# Data + metrics engine (deterministic source of truth)
pandas
numpy

# LLM — Anthropic direct (ADR-0008)
anthropic

# Exports
openpyxl            # .xlsx workbook

# Dev tooling / quality gates
pytest
pytest-cov
ruff
mypy
