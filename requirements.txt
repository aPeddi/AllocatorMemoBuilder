# ─────────────────────────────────────────────────────────────
# AllocatorMemoBuilder — dependencies.  Install with: ./amb setup
# (./amb setup also installs backend/ as the editable amb_core package)
# ─────────────────────────────────────────────────────────────

# Validation / config (structured outputs live here)
pydantic>=2
pydantic-settings
python-dotenv
pyyaml

# Data + metrics engine (deterministic source of truth)
pandas
numpy

# Live market data (FRED). requests bundles a CA cert store, which fixes the
# common macOS "SSL: CERTIFICATE_VERIFY_FAILED" that otherwise breaks live fetch.
requests
certifi

# LLM — Anthropic direct (ADR-0008)
anthropic

# Memo rendering / export
jinja2
openpyxl            # .xlsx export (Phase 2)

# API surface (Phase 2 — present so `./amb serve` is ready)
fastapi
uvicorn[standard]

# Dev tooling / quality gates
pytest
pytest-cov
ruff
mypy
